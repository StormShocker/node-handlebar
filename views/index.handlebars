<h1>Eat The Burger!</h1>

<h2>burgers that are not devoured!</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{>burgers/burger-block devoured=false}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that are no more!!</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block devoured=true}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a helpless burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="burName">Burger Name:</label>
    <input type="text" id="burName" name="name">
  </div>

  <div class="form-group">
    <label for="devoured">Devour Burger?</label><br>
    <input type="radio" name="devoured" value="1" checked> Not devoured<br>
    <input type="radio" name="devoured" value="0"> Devoured
  </div>

  <button type="submit">Add burger</button>
</form>